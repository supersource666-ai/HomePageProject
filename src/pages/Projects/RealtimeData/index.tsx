import React from 'react';

const RealtimeData: React.FC = () => (
  <div style={{ padding: 32 }}>
    <h2 style={{ fontSize: 28, fontWeight: 700, marginBottom: 16 }}>实时数据采集子系统</h2>
    <p>这里是实时数据采集子系统的页面内容。</p>
  </div>
);

export default RealtimeData;
